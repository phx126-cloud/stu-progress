# VPS 部署教程（学员进度管理网站）

> 这套方案最适合你：**关电脑也能用 + 固定网址 + 不用改代码 + 支付宝/微信付款不用信用卡**。
> 唯一的代价是每月几十块钱服务器租金（比 Render 绑卡、比自建省心）。

---

## 为什么选 VPS？

| 你的诉求 | VPS 是否满足 |
|---------|:---:|
| 关电脑也能用 | ✅ 代码跑在远程服务器，24h 常驻 |
| 固定网址 | ✅ 固定公网 IP（或绑域名变 `你的域名.com`）|
| 不绑信用卡 | ✅ 国内轻量服务器用**支付宝/微信**买 |
| 不用改代码 | ✅ 现在的 `server.js` 原样跑，不用改架构 |
| 飞书同步照常 | ✅ 飞书密钥放服务器环境变量即可 |

对比 Render（要绑卡）、Vercel（要改 Serverless）、本机隧道（关电脑就断），VPS 是对小白**最省心**的一条。

---

## 第 1 步：买一台轻量服务器

1. 打开 **阿里云** 或 **腾讯云** 官网（手机/电脑浏览器都行）
2. 搜 **「轻量应用服务器」** → 点「立即购买」
3. 选配置（照抄）：
   - **地域**：选 **中国香港** 或 **新加坡**（免备案，国内也能流畅访问）
   - **镜像/系统**：选 **Ubuntu 22.04 LTS**
   - **配置**：最低 **1 核 1G** 就够（访问量小）
   - **带宽**：4~6 Mbps 足够
4. 付款：**支付宝 / 微信**扫码，约 **30~60 元/月**（年付更便宜）
5. 下单后等 1~2 分钟，控制台里能看到你的服务器

> 香港/新加坡节点**不用 ICP 备案**，买完直接用。如果选大陆节点，域名要备案，麻烦，不推荐新手。

---

## 第 2 步：拿到 IP 和设密码

1. 云控制台 → 「轻量应用服务器」→ 点进你的实例
2. 记下 **「公网 IP」**（一串数字，如 `123.45.67.89`）
3. 左侧「重置密码」→ 设一个 root 密码（记好，后面要输）
4. 确认「防火墙」里**放行端口** `10000`（后面要用，现在先记着，第 8 步细说）

---

## 第 3 步：SSH 连上服务器

你用的是 Mac，自带终端，不用装软件：

1. 打开 **「终端」**（启动台 → 其他 → 终端）
2. 输入下面这行（把 IP 换成你的）：
   ```bash
   ssh root@123.45.67.89
   ```
3. 第一次会问 `Are you sure...`，输入 `yes` 回车
4. 提示输密码，粘贴你刚设的 root 密码（输入时不显示，正常），回车
5. 看到 `root@xxxx:~#` 这样的提示符，说明**连上了**

---

## 第 4 步：装 Node.js

在服务器终端里，一行一行粘下面命令（每行回车）：

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
```

装完验证：
```bash
node -v
```
能看到 `v20.x.x` 就成功了。

---

## 第 5 步：拉取网站代码

```bash
apt-get install -y git
git clone https://github.com/phx126-cloud/stu-progress.git
cd stu-progress
```

> 如果你的 GitHub 仓库是私有的，clone 会要账号密码——用公开的那个仓库即可（之前推的就是公开的）。

---

## 第 6 步：填飞书密钥（关键）

飞书三项 + 访问口令要放到服务器上（**不要写进代码**，放环境变量最安全）：

```bash
nano .env
```

在里面粘贴（把值换成你自己的真实值）：

```env
FEISHU_APP_ID=你的cli_开头那串
FEISHU_APP_SECRET=你的AppSecret
FEISHU_APP_TOKEN=你的X开头那串
ACCESS_PASSWORD=3a3ed5efb1c88e27
PORT=10000
```

- 按 `Ctrl+O` 回车保存，再按 `Ctrl+X` 退出
- 飞书三项怎么拿：回飞书开放平台 → 你的应用 → 凭证页复制 App ID / App Secret；多维表格 `app_token` 从表格网址 `/base/` 和 `?` 之间复制
- `ACCESS_PASSWORD` 就是网页登录口令 `3a3ed5efb1c88e27`

---

## 第 7 步：安装依赖 + 常驻运行

```bash
npm install
npm install -g pm2
pm2 start server.js --name stu
pm2 save
pm2 startup
```

- `pm2` 会让服务**开机自启、崩了自动重启**，你不用担心关终端就断
- 看到 `stu` 显示 `online` 就成功了

---

## 第 8 步：放行防火墙端口

回到云控制台 → 你的服务器 → **「防火墙」** → 添加规则：
- 应用类型：自定义
- 协议：TCP
- 端口：`10000`
- 策略：允许

保存后，浏览器访问：

```
http://你的IP:10000
```

（例如 `http://123.45.67.89:10000`）→ 输访问口令 `3a3ed5efb1c88e27` → 就能用了，**关你电脑也不影响**。

---

## 第 9 步（可选）：绑域名，变成固定网址

不想记 IP、想用 `你的名字.com` 这种固定网址：

1. 买个域名（阿里云/腾讯云几元~几十元/年，如 `xxx.top`）
2. 域名控制台 → 解析 → 添加 **A 记录**：主机名 `@` / `www`，指向你的服务器 IP
3. 等几分钟生效，浏览器开 `http://你的域名.com:10000` 即可

> 想去掉 `:10000` 端口、还能 HTTPS 加密，可在服务器上装 **Caddy** 反代（进阶，需要再说）。

---

## 验证是否成功

1. 打开 `http://IP:10000`（或你的域名）
2. 输访问口令 → 进入
3. 网页上新增/拖拽一条投递 → 去飞书多维表格看是否同步
4. 在飞书表格改一个值 → 网页 15 秒内自动刷新

两边同步正常，部署完成。

---

## 花费小结

| 项目 | 费用 |
|------|------|
| 轻量服务器（1核1G）| 约 30~60 元/月（年付更低）|
| 域名（可选）| 几元~几十元/年 |
| 总计 | 每月几十块，完全可控 |

---

## 常见问题

**Q：连不上 SSH？**
A：检查 IP 对不对、密码对不对；云控制台「防火墙」是否放行了 22 端口（默认放行）。

**Q：打开网址显示飞书未配置？**
A：第 6 步 `.env` 的飞书值没填对/漏填。重新 `nano .env` 核对三项和口令，然后 `pm2 restart stu`。

**Q：端口访问不了？**
A：云控制台「防火墙」没放行 `10000`，回去第 8 步加规则。

**Q：以后怎么更新代码？**
A：服务器上 `cd stu-progress && git pull && pm2 restart stu` 即可。
