# 飞书多维表格同步 · 详细配置指南

> 这份指南手把手带你把网站连上飞书。只要按步骤走，第一次配置大约 **10 分钟**（后续换电脑 3 分钟）。
> 配置完成后：
> - 你在**飞书多维表格**里改的数据，网页会**每 15 秒自动同步刷新**；
> - 你在**网页**上的新增 / 编辑 / 拖拽看板操作，也会**实时写回飞书表格**；
> - 手机上直接用飞书 App 就能随时改数据，电脑网页自动跟着变。

---

## 整体原理（先看 30 秒，心里有数）

```
     你（飞书 App / 网页）                你的电脑
  ┌───────────────────┐            ┌──────────────────────────┐
  │  飞书多维表格       │ ←云端 API→ │  启动服务.command          │
  │ （唯一数据源）      │            │  （server.js，端口 3210）  │
  └───────────────────┘            │          ↑↓ 双向同步        │
                                   │   浏览器打开 localhost:3210 │
                                   └──────────────────────────┘
```

- 飞书表格是**唯一的数据源**，网页只是它的"镜子 + 操作面板"。
- 因为你自己的电脑要帮网页和飞书"对话"（飞书不允许网页直接连），所以需要一个**本地小服务** `server.js` 常驻运行。
- 这个服务**不会**把你的数据发到任何第三方，只在你本机和飞书之间传输。

---

## 第 1 步：进入飞书开放平台，创建自建应用

1. 用**电脑浏览器**打开：https://open.feishu.cn/app
   - 如果还没登录，会先跳到飞书登录页，用你的飞书账号登录即可。
2. 页面右上角点 **「创建企业自建应用」**（青绿色按钮）。
3. 在弹出的创建窗口里填：
   - **应用名称**：随便填，建议叫 `学员进度管理`（你以后在飞书里会看到这个名字）
   - **应用描述**：可留空，或写 `求职辅导学员进度管理同步`
   - **应用图标**：随便选一个
4. 点 **「创建」**。
5. 创建成功后**自动进入应用详情页**。

> ⚠️ 注意：必须是「**企业自建应用**」，不要选「商店应用」或「个人应用」。企业自建应用才能访问多维表格。

---

## 第 2 步：复制凭证（App ID / App Secret）

在应用详情页左侧菜单里：

1. 点 **「基础信息」→「凭证与基础信息」**。
2. 右侧你会看到两行关键信息：
   - **App ID**：形如 `cli_a8b9c0d1e2f3g4h`。点右边的「复制」按钮。
   - **App Secret**：默认是 `••••••••`。点 **「查看」**（或「显示」），输入登录密码/验证码后，点复制。
3. **先把这两个值临时贴到记事本里**，第 5 步要填进 `config.json`。

> 🔒 安全提示：App Secret 相当于应用的密码，不要发给别人，也不要截图发群里。

---

## 第 3 步：开通多维表格权限并发布版本

飞书要求应用"申请了权限 + 发布版本"之后，才能真的去读你的表格。两步都要做。

### 3.1 申请权限

1. 应用详情页左侧菜单点 **「权限管理」**。
2. 在顶部搜索框输入 `bitable`，点放大镜搜索。
3. 在结果里找到 **「查看、评论、编辑和管理多维表格」**（权限代码 `bitable:app`），把它的开关打开（变成蓝色）。
   - 名字里可能带 `（bitable:app）`，确认代码是 `bitable:app` 就对了。
4. 页面最下方通常会让你勾选"权限使用范围"，选 **「全部员工」** 或 **「仅指定部门/成员」** 都行（你自己能访问即可）。

### 3.2 创建并发布版本（非常关键，很多人漏这步）

1. 权限申请完，页面右上角点 **「创建版本」**（蓝色按钮）。
2. 在弹出的版本窗口：
   - **版本号**：填 `1.0.0`
   - **更新说明**：填 `学员进度管理同步`
   - 应用可用范围：保持默认（你自己）
3. 点 **「保存」**，然后点 **「发布」**。
4. 一般情况企业自建应用会**自动通过**，几秒后状态变成"已发布"。
   - 若提示"需管理员审批"，找你们飞书管理员在后台点一下通过即可（你自己是管理员的话会直接通过）。

> ❗ 如果只开权限不点"创建版本 + 发布"，网页连飞书时会报 `99991672 无权限` 错误。

---

## 第 4 步：新建一个飞书多维表格，拿到 app_token

1. 打开飞书桌面客户端（或网页版 https://www.feishu.cn ），进入 **云文档**。
2. 点 **「新建」→「多维表格」**，新建一个空表，名字随便（如 `学员进度管理台账`）。
   - 新建后表里默认会有个示例数据表，不用管，我们的服务会**自动新建**三张表，不会用这个默认的。
3. 打开这个多维表格，看浏览器**地址栏**，网址类似：
   ```
   https://v3ohosn4s3.feishu.cn/base/XaBcDeFgHiJkLmNoPqRsTuVw?table=tblxxx&view=vewxxx
   ```
4. **`/base/` 和 `?` 之间那串字符就是 `app_token`**。
   - 上面例子里是：`XaBcDeFgHiJkLmNoPqRsTuVw`
   - ⚠️ **只复制 `G` 开头到 `?` 之前的部分**，不要带 `?table=...` 那些。

> 小技巧：地址栏里把 `?` 及后面全部删掉，剩下的 `/base/` 后面那串就是 app_token。

---

## 第 5 步：把应用"添加进"这个多维表格（最容易漏！）

> 这一步是**最高频的报错原因**。光有 app_token 还不够，必须让这个应用成为该表格的"协作者"，否则飞书会报 `91402 / NOTEXIST`。

在飞书里打开第 4 步建好的多维表格：

**方法 A（推荐）：添加文档应用**
1. 点表格右上角的 **「···」（更多）**。
2. 在菜单里找 **「添加文档应用」**（有的版本叫「添加应用」）。
3. 在搜索框输入你**第 1 步建的应用名**（如 `学员进度管理`）。
4. 搜到后点 **「添加」**。
5. 添加后，确认该应用对表格的权限是 **「可编辑」**（不是"仅查看"）。

**方法 B（若找不到"添加文档应用"入口）：通过分享添加**
1. 点表格右上角 **「分享」**。
2. 在"添加协作者"里搜索你第 1 步的应用名。
3. 权限选 **「可编辑」**，点添加。

添加成功后，应用会作为"成员"出现在这个表格的协作者列表里。

---

## 第 6 步：把三项凭证填进 config.json（逐格拆解）

这一步走完，网页就能拿到飞书的"钥匙"。下面一步一步来。

### 6.1 找到并打开 config.json

1. 打开**访达（Finder）**，进入本项目的文件夹（默认在 `学员进度管理/` 目录下）。
2. 找到文件 **`config.json`**（图标是个蓝色 JSON 文档）。
3. **右键**它 →「打开方式」→ 选 **「文本编辑」**（TextEdit，Mac 自带），或用 **VS Code / 记事本** 打开也行。
   - 不要直接双击，双击有时会用别的程序打开导致不方便改。
4. 打开后你看到的初始内容应该长这样：

```json
{
  "app_id": "填入你的 App ID",
  "app_secret": "填入你的 App Secret",
  "app_token": "填入你的多维表格 app_token",
  "port": 3210
}
```

### 6.2 逐项填值（对照你前面记下来的三样东西）

下面每一项，都对应你之前在某个步骤里复制下来、存在记事本里的那串字符。**只替换双引号 `"` 里面的文字**，外面的引号和逗号都别动。

**① `app_id`**
- 来源：第 2 步复制的 App ID（形如 `cli_a8b9c0d1e2f3g4h`）。
- 改完应该像：`"app_id": "cli_a8b9c0d1e2f3g4h"`
- ⚠️ 注意：App ID 是 `cli_` 开头的一长串；如果看到自己填的是 `cli_` 后面没字符，说明只填了占位符。

**② `app_secret`**
- 来源：第 2 步点"查看"后复制的 App Secret（一长串无规律的字母数字）。
- 改完应该像：`"app_secret": "1a2B3c4D5e6F7g8H9i0J..."`
- ⚠️ App Secret 很容易复制不完整（尾巴被截断）或首尾多带空格。填完检查一下开头和结尾几个字符对不对得上。

**③ `app_token`**
- 来源：第 4 步从多维表格网址里截取的那串（形如 `XaBcDeFgHiJkLmNoPqRsTuVw`）。
- 改完应该像：`"app_token": "XaBcDeFgHiJkLmNoPqRsTuVw"`
- ⚠️ 最容易错：多复制了 `?table=` 后面的参数，或带了 `/base/` 前面的域名。正确格式是 `/base/` 和 `?` 之间那一截（开头字母可能是 `X`、`G` 等，以你网址实际显示为准，不要凭记忆补前缀）。

**④ `port`** —— 一般**不用改**，保持 `3210`。
- 只有当你确定 3210 被别的程序占用、网页打不开时，才改成 3300、8080 等。

### 6.3 填完后的完整示例

```json
{
  "app_id": "cli_a8b9c0d1e2f3g4h",
  "app_secret": "1a2B3c4D5e6F7g8H9i0JkLmN",
  "app_token": "XaBcDeFgHiJkLmNoPqRsTuVw",
  "port": 3210
}
```

### 6.4 JSON 格式避坑（很重要）

配置文件是 **JSON 格式**，错一个标点服务就起不来。请逐条核对：

- ✅ 每个值都要用**英文双引号 `"`** 包住（不是中文引号 `"` 或 `'`）。
- ✅ 四条配置之间用**英文逗号 `,`** 隔开；**最后一条 `port` 后面不要加逗号**。
- ✅ 不要在文件最前面加 `// 注释` 或 `备注：`，JSON 不支持注释，加了会报错。
- ❌ 不要换行把 `"app_id"` 和值拆开写到两行（虽然有些解析器能容错，但别冒险）。
- ❌ 不要出现多余的空格在引号**外**的值里（比如 `" cli_xxx"` 前面多空格会导致不匹配）。

**快速自检方法**：复制整段内容，粘贴到在线 JSON 校验网站（如 https://jsonlint.com ）或 VS Code 里，若没报红就说明格式正确。

### 6.5 保存

1. 在文本编辑里按 **⌘S**（或「文件 → 存储」）。
2. 确认文件**还叫 `config.json`**、**还在这个文件夹里**（不要存成 `config.json.txt`！Finder 默认会隐藏扩展名，可在「显示 → 显示文件扩展名」里确认）。
3. 关闭文件。

> ❌ 错误示例（服务会判定"未配置"）：
> `"app_id": "填入你的 App ID"` —— 还留着占位文字。
> ✅ 正确示例：
> `"app_id": "cli_a8b9c0d1e2f3g4h"` —— 真实值，无占位。

填好后回到 **第 7 步** 双击「启动服务.command」即可。若启动时终端报错，把红字发我。

---

## 第 7 步：启动服务，完成首次同步

### macOS 用户（双击即可）
1. 双击本文件夹里的 **`启动服务.command`**。
   - 首次双击若被系统拦截，右键它 → **「打开」**，在弹窗里点「打开」即可。
2. 会自动弹出一个终端窗口，并自动打开浏览器到 http://localhost:3210 。
3. **首次运行**会在你的飞书多维表格里**自动创建三张表**（见下方"自动建好的表结构"），稍等几秒。

### 其他系统 / 手动启动
1. 确保电脑装了 Node.js（https://nodejs.org ，装 LTS 版即可）。
2. 在文件夹里打开终端，运行：
   ```bash
   node server.js
   ```
3. 看到终端打印 `同步服务已启动` 后，浏览器访问 http://localhost:3210 。

### 验证成功
- 网页**左下角**显示 **「🟢 已连接飞书表格」** → 成功！
- 同时，回飞书看那个多维表格，里面多了三张表：**学员、职位、投递**。
- 如果左下角显示「本地模式 / 未连接」，说明 config.json 没填对或第 5 步没做，见下方排查。

---

## 自动建好的表结构（了解即可，不用手建）

服务首次连接成功后，会在你的多维表格里自动生成下面三张表，字段和下拉选项都已配好：

### 表 1：学员
| 字段 | 类型 | 说明 / 下拉选项 |
|---|---|---|
| 姓名 | 文本 | 必填，是和"投递"表关联的钥匙 |
| 联系方式 | 文本 | 微信 / 手机等 |
| 学校 | 文本 | |
| 专业 | 文本 | |
| 目标岗位 | 文本 | |
| 辅导阶段 | 单选 | `咨询中` / `辅导中` / `求职中` / `已上岸` / `暂停` |
| 简历状态 | 单选 | `待优化` / `优化中` / `已定稿` |
| 简历链接 | 文本 | 在线简历 URL |
| 作品集 | 文本（多行） | 每行一个作品，格式 `作品名\|链接`，见下 |
| 备注 | 文本 | |
| 加入日期 | 文本 | 形如 `2026-07-30` |

### 表 2：职位
| 字段 | 类型 | 说明 / 下拉选项 |
|---|---|---|
| 公司 | 文本 | 必填，和"投递"表关联的钥匙 |
| 职位名称 | 文本 | 必填，和"投递"表关联的钥匙 |
| 城市 | 文本 | |
| 薪资 | 文本 | |
| 渠道 | 文本 | 内推 / 校招 / 直投 等 |
| 状态 | 单选 | `招聘中` / `已关闭` |
| JD链接 | 文本 | |
| 备注 | 文本 | |

### 表 3：投递
| 字段 | 类型 | 说明 / 下拉选项 |
|---|---|---|
| 学员姓名 | 文本 | 必须和"学员"表里的姓名**完全一致**（含字） |
| 公司 | 文本 | 必须和"职位"表里的公司**完全一致** |
| 职位名称 | 文本 | 必须和"职位"表里的职位名称**完全一致** |
| 阶段 | 单选 | `待投递` / `已投递` / `笔试` / `面试` / `Offer` / `未通过` |
| 投递日期 | 文本 | 形如 `2026-07-30` |
| 下一步行动 | 文本 | 提醒事项 |
| 备注 | 文本 | |
| 更新日期 | 文本 | 自动更新 |

> **关联机制说明**：投递表是靠「学员姓名 + 公司 + 职位名称」三个文本去匹配学员表和职位表的，**所以在这三列手填时务必和另外两张表里的字一模一样**（多一个空格都不行）。在网页上点"新建投递"会自动带上正确的值，无需手填。

---

## 第 8 步（可选）：把旧数据从网页搬到飞书

如果你之前在**本地模式**（没连飞书时）已经在网页里录过学员/职位/投递数据：

1. 连接成功（左下角显示已连接）后，点左下角 **「☁️ 迁移本地数据到飞书」**。
2. 弹窗确认后，本地数据会**复制**一份到飞书表格（本地数据仍保留，不会删除）。
3. 迁移完刷新一下，网页数据就完全来自飞书了。

> 如果之前没录过数据，跳过这步，直接去网页里新增即可。

---

## 日常使用说明

- **启动**：每次要用网页，先双击 `启动服务.command`（保持那个终端窗口开着），再开浏览器访问 http://localhost:3210 。
- **关闭**：不用时直接关掉那个终端窗口即可。
- **自动同步**：网页打开期间，每 15 秒会自动拉取飞书最新数据；你正在编辑（弹窗/抽屉打开）时会暂停刷新，关掉后立刻恢复。
- **双向**：网页上新增/修改/拖拽看板，会立刻写回飞书；飞书里改了，网页最多 15 秒后自动更新。

---

## 在飞书里手填数据的注意事项

| 做啥 | 注意点 |
|---|---|
| 新增学员 | 姓名必填；作品集每行一个，`作品名称\|链接`，多个作品换行写 |
| 新增职位 | 公司、职位名称必填（这是投递关联的钥匙） |
| 新增投递 | 学员姓名 / 公司 / 职位名称 要和另两张表**逐字一致** |
| 阶段/状态列 | 用现有下拉选项，不要自己敲新名字（网页不认） |
| 日期列 | 写 `2026-07-30` 这种格式最稳 |
| 删除行 | 在飞书里删行没问题，网页下次同步会自动去掉 |

---

## 常见问题排查（对照症状找原因）

### ❶ 网页左下角一直显示「本地模式 / 未连接飞书」
原因按概率排序：
1. `config.json` 三项没填全，或还残留占位示例（`填入`/`你的`/`xxxx`/`cli_`）→ 回去第 6 步检查。
2. 改完 config.json 后**没重启服务** → 关掉终端窗口，重新双击 `启动服务.command`。
3. `启动服务.command` 没正常运行 → 看终端窗口有没有报错，贴出来。

### ❷ 报错 `91402` 或 `NOTEXIST`
→ **第 5 步没做**：应用还没被添加进这个多维表格。回到第 5 步把应用加为可编辑协作者。
也可能 app_token 复制错了（带了 `?` 后面参数）→ 回去第 4 步重复制。

### ❸ 报错 `99991672` 或 `无权限 / permission denied`
→ 第 3 步没做完整：要么没开 `bitable:app` 权限，要么开了但**没点"创建版本 + 发布"**。回去第 3 步补发布。

### ❹ 报错 `app_id / app_secret 错误`
→ App ID 或 App Secret 复制错了（多了空格、少了几位、或把两个搞反）。回去第 2 步重新复制，注意 App Secret 要点"查看"后才显示真实值。

### ❺ 飞书里改了，网页没马上变
→ 正常，最多等 15 秒。若你正打开某个编辑弹窗/详情抽屉，刷新会暂停，关掉即恢复。

### ❻ 网页上看不到某条投递 / 关联不到学员
→ 投递表的"学员姓名/公司/职位名称"和另外两张表**字没对上**（比如多了空格、简繁体不同、多了标点）。在网页看板里直接拖拽/编辑会自动补全正确值，最稳妥。

### ❼ 换电脑 / 重装系统后用不了
→ 把整个「学员进度管理」文件夹拷到新电脑，装好 Node.js，填好 config.json，双击 `启动服务.command` 即可。飞书表格里的数据不需要动。

### ❽ 端口 3210 被占用，网页打不开
→ 改 `config.json` 里的 `"port": 3210` 为别的（如 3300），保存后重启服务，访问 http://localhost:3300 。

---

## 一句话总结流程

创建自建应用 → 复制 App ID/Secret → 开通 bitable 权限并发布 → 新建多维表格拿 app_token → 把应用添加进表格（可编辑）→ 填 config.json → 双击启动服务 → 自动建表、开始同步。

遇到任何报错，把**左下角状态文字**或**终端窗口红字**发给我，我帮你定位。
