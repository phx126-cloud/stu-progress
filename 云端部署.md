# 上云部署指南（不管你懂不懂都能弄）

本文件夹已经是**云端就绪**版本：飞书三项凭证改读环境变量，不依赖你本机文件。
照下面三步，就能在任意云平台（Render / Railway / 自己的 VPS）24 小时常驻。

## 第一步：把代码推到 Git 仓库（一次性）
在你电脑上打开「终端」，进到本文件夹：
```bash
cd /Users/penghaoxuan/WorkBuddy/2026-07-30-16-45-45/学员进度管理
git init && git add . && git commit -m "学员进度管理-云端版"
git branch -M main
git remote add origin <你的仓库地址>   # 在 GitHub/GitLab 建个空仓库后粘贴地址
git push -u origin main
```

## 第二步：在云平台填三个环境变量
去 Render（render.com）或 Railway（railway.app）注册个免费账号，新建一个 **Web Service / Node 服务**，
「环境变量」里填这三项（值同你 `config.json` 里的一致）：
```
FEISHU_APP_ID=cli_aae19f55d078dbef
FEISHU_APP_SECRET=
FEISHU_APP_TOKEN=XeHHwKaD3irYdNkQCrXckU3tnrC
ACCESS_PASSWORD=3a3ed5efb1c88e27
PORT=3210
```

## 第三步：部署并常驻
- **Render**：连上 GitHub 仓库后，把本文件夹里的 `render.yaml` 当 Blueprint 读入，点 Deploy 即可。
- **Railway**：上传本文件夹，它读 `railway.json` 自动起 `node server.js`。
- **自己的 VPS**：把文件夹拷过去，`node server.js` 起服务，再用 systemd 守护（见下方「VPS 常驻」）。

部署完，公开网址就是 `https://你的域名` 或平台给的 `.onrender.com` / `.railway.app` 子域——
**网址固定不变、电脑关机也不影响**，学员/同事拿「网址 + 访问口令」就能用。

## VPS 常驻（如果你有台一直开着的服务器）
```bash
# 建一个 systemd 守护，机器重启也自动跑
sudo tee /etc/systemd/system/student-progress.service > /dev/null <<'EOF'
[Unit]
Description=学员进度管理同步服务
After=network.target
[Service]
WorkingDirectory=/path/to/学员进度管理
ExecStart=/usr/bin/node server.js
Restart=always
[Install]
WantedBy=multi-user.target
EOF
sudo systemctl enable --now student-progress
```

## 本地开发（不改云端）
不填环境变量时，服务自动读本机 `config.json`（你现在就在用这个）。
直接 `node server.js` 即可，和之前一模一样。
